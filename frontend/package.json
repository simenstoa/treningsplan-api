{
  "name": "treningsplan",
  "version": "1.0.0",
  "description": "Lage din egen treningsplan",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "start": "ELM_DEBUGGER=false elm-app start",
    "build": "elm-app build",
    "api": "elm-graphql https://treningsplan-api.s33.no --base Treningsplan",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "create-elm-app": "^3.2.2"
  },
  "devDependencies": {
    "@dillonkearns/elm-graphql": "^3.6.2"
  }
}
